# URL of the deployed Backend REST API server (e.g. on Render, Railway, or VPS)
BACKEND_URL=https://your-backend-domain.com

# Port for Frontend UI client (default: 3000)
PORT=3000

# Node environment
NODE_ENV=production
